# Dev workflow with live backend
- Skip final build checks at task end unless user explicitly requests them
- Backend is already running by user at `http://localhost:3000`
- Avoid commands that disrupt local runtime flow during active UI work
