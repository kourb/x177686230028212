# Project design rules added
- Added `.xai/rules/design-delivery.md` as hard project rule set for UI implementation
- Goal: prevent layout drift, mockup framing, overflow regressions, and iterative alignment churn
- Rule enforces Figma-first deterministic implementation and pre-response visual quality checks
