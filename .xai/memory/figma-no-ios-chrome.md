# Figma web layout rule
- When implementing web pages from mobile Figma frames, do not include iOS chrome elements
- Exclude top iOS status/header area and bottom home indicator area from rendered layout
- Treat them as device framing, not product UI
