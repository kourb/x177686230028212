# Web app scope + scalar source
- Project implementation scope is web application first (not iOS/admin at this stage)
- Product requirements working summary is stored at `doc/summary.txt`
- Scalar API connection data is stored in `doc/scalar.txt`
- Use Scalar credentials only from source file, do not duplicate secrets across project docs
